# PieroloOS package resolution repair

This replacement package contains the project's supplied `package.json` and
scripts for generating the authoritative npm lockfile.

A real package-lock.json is intentionally generated by npm rather than
fabricated. This preserves the integrity hashes and complete transitive
dependency graph required by `npm ci`.
